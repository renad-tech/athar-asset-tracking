# المرحلة ١ — ما الذي تغيّر؟

## ⚠️ اقرأ هذا أولاً
1. **غيّر مفتاح Anthropic** — المفتاح القديم انكشف في ملف zip. أنشئ واحد جديد من console.anthropic.com
2. **ملف `.env` غير موجود في هذه النسخة** (حذفته عمداً للأمان). أنشئه بنفسك:
   - انسخ `.env.example` وسمّه `.env`
   - الصق فيه رابط ومفتاح Supabase + مفتاح Anthropic الجديد

## 1) شغّل تحديث قاعدة البيانات
افتح Supabase ← SQL Editor ← الصق **كامل** محتوى `supabase_schema.sql` ← Run.
أضفنا جدول `employees` (الموظفون المصرح لهم) + ٣ موظفين تجريبيين.
الملف آمن للتشغيل أكثر من مرة، لن يحذف بياناتك.

## 2) ثبّت المكتبة الجديدة
```
npm install
```
أضفنا `@supabase/supabase-js` — بدونها لن يعمل أي اتصال بقاعدة البيانات.

## 3) شغّل
```
npm run dev
```
افتح **http://localhost:8443** فقط.
(المنفذ 8443 وليس 5173 — إعدادات Vite في المشروع تفرضه.)

---

## ✅ ما تم إصلاحه في هذه المرحلة

**١. الكاميرا**
الكود القديم كان يبتلع كل الأخطاء ويكتب "denied" مهما كان السبب. الآن يعرض السبب الحقيقي بالعربي + زر "إعادة المحاولة".
السبب الأشهر: المتصفح **يمنع الكاميرا تماماً** إلا على `localhost` أو `https://`.
- ✅ `http://localhost:8443`
- ❌ الضغط على `index.html` مباشرة (`file://`)
- ❌ فتحه عبر عنوان الشبكة مثل `http://192.168.1.5:8443`
- ❌ داخل معاينة Figma Make

إذا ظهر "تم رفض إذن الكاميرا": اضغط القفل 🔒 بجانب العنوان ← Camera ← Allow ← حدّث الصفحة.

**٢. التنقل كموقع حقيقي**
حذفنا السحب والأسهم بالكامل. الآن شريط تنقل علوي بتبويبات قابلة للنقر.
التبويبات مقفلة حتى تسجّل الدخول (ما عدا "الموظفون").

**٣. الوقت والتاريخ**
ساعة حيّة تتحدث كل ثانية في شريط التنقل وفي رأس كل صفحة.

**٤. صفحة الموظفين (جديدة)**
تبويب "الموظفون": أضف موظف (الرقم الوظيفي، الاسم، المسمى، القسم، **صورة**) ← يُحفظ فعلياً في Supabase والصورة تُرفع إلى Storage. مع قائمة بالموظفين وإمكانية الحذف.
هذا هو السجل الذي سيطابق عليه الذكاء الاصطناعي في المرحلة ٢.

**٥. ملف المشرف من قاعدة البيانات**
عند تسجيل الدخول يتحقق النظام فعلياً من جدول `supervisors`، ثم يجلب الملف الكامل ويعرضه في كل الصفحات.
⚠️ يعني: الرقم الوظيفي يجب أن يكون **موجوداً في الجدول** وإلا سيُرفض. الرقم التجريبي: `BADGE-2024-0849`

**٦. الاتصال الحقيقي**
`BACKEND.enabled` و `AI.enabled` صارا يشتغلان تلقائياً بمجرد وجود المفاتيح في `.env` — لا تعديل يدوي.

---

## ⏭️ المرحلة ٢ (القادمة)
كشف الأشخاص · مسح بطاقة QR والتعرف على الموظف من قاعدة البيانات · تتبع الأجسام وكشف التحريك (صورة قبل/بعد) · سجل نشاط يتحكم فيه الذكاء الاصطناعي · إخفاء الألوان إلا عند كشف فعلي
